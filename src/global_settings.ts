export let backgroundColor = getComputedStyle(document.body).getPropertyValue(
    "background-color"
);

export let fontColor = "rgb(220, 220, 220)";

/**how many of the previous data states are shown*/
export let dataTrace = 0;
